/**
 * Типографика /leader — та же, что у всего сайта.
 *
 * Здесь был свой полный набор: `Display`, `SectionHeading`, `Lead`, `Body`
 * и прочие, повторяющие компоненты из `components/ui` слово в слово. Две
 * копии одного набора расходятся при первой же правке — одну из них
 * поправят, вторую забудут, — поэтому осталась только пересылка.
 *
 * Имена сохранены: секции страницы зовут `ShimmerText` и `Label`, и
 * переименовывать их по всей вёрстке ради вывески незачем.
 */
export {
  Display,
  CardTitle,
  Lead,
  Body,
  SectionHeading,
  /** Ключевое слово в заголовке — курсив Playfair электриком. */
  ItalicAccent as ShimmerText,
  /** Надзаголовок секции: разрядка в капитель с чертой слева. */
  Eyebrow as Label,
} from "@/components/ui";
