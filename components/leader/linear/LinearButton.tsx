/**
 * Кнопка /leader — та же, что у всего сайта.
 *
 * Своя реализация здесь была ровно до тех пор, пока /leader жил на
 * отдельной тёмной палитре. Палитра стала общей, силуэт кнопки — тоже
 * (капсула, Inter 500, блик на ховере), и держать вторую копию значит
 * заново разводить их при первой правке.
 *
 * `external` больше не параметр: внешние ссылки определяются по самому
 * href — `/` и `#` ведут внутрь, остальное открывается в новой вкладке.
 */
export { Button as LinearButton } from "@/components/ui";
/* Тихая ссылка идёт из того же места и по той же причине: на /leader она
   стоит второй парой к кнопке в первом экране, как на /proryv и
   /individual. */
export { QuietLink } from "@/components/ui";
