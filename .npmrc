# Ставим из публичного npm.
# Глобальный ~/.npmrc этой машины смотрит в приватный AWS CodeArtifact другого
# проекта, и его токен сюда не относится — без этой строки npm install падает 401.
registry=https://registry.npmjs.org/
