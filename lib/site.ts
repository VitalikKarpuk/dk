/**
 * Константы сайта: автор, соцсети, реквизиты.
 *
 * Раньше они жили в `components/leader/course.ts` — рядом с номером
 * потока и датой старта, то есть в файле одной страницы. Подвал главной
 * при этом писал те же реквизиты своими литералами, и год в копирайте
 * у него считался от `Date.now()`, а на /leader был вписан строкой:
 * две версии одних и тех же данных, расходящиеся молча.
 *
 * Здесь — то, что не зависит от программы и одинаково на всех страницах.
 */

export const AUTHOR_NAME = "Дарьей Карпук";
export const AUTHOR_NAME_GENITIVE = "Дарьи Карпук";
export const AUTHOR_ROLE = "коуч, психолог";
export const AUTHOR_COMPANY = "ИП Карпук Д.В.";
export const AUTHOR_TAX_ID = "УНП 291787139";
export const COPYRIGHT_YEARS = "2020–2026";

export const INSTAGRAM_URL = "https://instagram.com/darya_karpuk.psy";
export const INSTAGRAM_HANDLE = "darya_karpuk.psy";
export const YOUTUBE_URL = "https://www.youtube.com/@daria_karpuk.psy1";
export const YOUTUBE_HANDLE = "daria_karpuk.psy";

/** Соцсети подвала. Порядок — тот же, что в шапке и на карточках. */
export const SOCIAL_LINKS = [
  { kind: "instagram", href: INSTAGRAM_URL, handle: INSTAGRAM_HANDLE },
  { kind: "youtube", href: YOUTUBE_URL, handle: YOUTUBE_HANDLE },
] as const;

/** Строка о практике — короткая, для подвала и OG-описаний. */
export const AUTHOR_BLURB =
  "Дипломированный психолог и коуч, автор программ по профессиональному и финансовому росту, спикер республиканских проектов.";

/**
 * Программы — то же меню в шапке и в подвале на всех страницах.
 *
 * Раньше переходов между страницами не было вовсе: с подстраницы вела
 * только ссылка «На главную», а на главной программы жили внутри плитки
 * в середине полосы. Общее меню — половина ощущения одного сайта.
 */
export const PROGRAM_LINKS = [
  { href: "/leader", label: "Я ЛИДЕР" },
  { href: "/proryv", label: "ПРОРЫВ" },
  { href: "/basic-laws", label: "Базовые законы" },
  { href: "/individual", label: "Консультации" },
] as const;

/**
 * Кнопка в шапке — одна на все страницы, включая /leader.
 *
 * Не CTA страницы, а вход в воронку: с любого лендинга человек попадает
 * в одно и то же место. У страницы своя кнопка («Записаться», «Купить
 * курс») остаётся в теле — в шапке она бы дублировала соседнюю секцию и
 * менялась от страницы к странице, а шапка должна читаться одинаково.
 *
 * Адрес абсолютный и на самой главной: `next/link` с якорем на текущем
 * маршруте доскроллит до секции, а один литерал на весь сайт не разъедется.
 */
export const DIAGNOSTIC_CTA = {
  href: "/#diagnostic",
  /* Глагол, а не название раздела. «Диагностика» — существительное, и в
     одном ряду с «Я ЛИДЕР», «ПРОРЫВ», «Базовые законы» кнопка читалась
     четвёртым пунктом меню, а не действием. Слово то же, что на кнопке
     в самой секции («Записаться бесплатно»): человек видит, что попал
     туда, куда нажимал. */
  label: "Записаться",
} as const;

/**
 * Форма записи на диагностику — последний шаг той же воронки: кнопка
 * шапки ведёт в секцию `#diagnostic`, кнопка секции — сюда.
 *
 * Адрес здесь, рядом с кнопкой, а не в вёрстке секции. Раньше он был
 * вписан литералом в `components/Diagnostic`, рядом лежала неиспользуемая
 * константа с тем же адресом в другой записи (`/d/<id>/` вместо
 * `/d/e/<id>/`), и третья копия — в `app/basic-laws`, тоже мёртвая. Три
 * записи одного адреса; поменяв «ту, которая называется FORM_URL», не
 * поменял бы ничего.
 */
export const DIAGNOSTIC_FORM_URL =
  "https://docs.google.com/forms/d/e/1FAIpQLSeifCxZg3TOYUceGHvoYSTDsk3ItvOZG5Ll-TJWFEEQynBu-w/viewform";

/**
 * Правила индивидуальной работы — постоянный адрес документа.
 *
 * Не в `PROGRAM_LINKS`: это не программа, и в одном ряду с «Я ЛИДЕР» и
 * «ПРОРЫВ» пункт читался бы пятым лендингом. Место ссылки — правовая
 * полоса подвала, рядом с оговоркой ниже.
 */
export const RULES_LINK = { href: "/rules", label: "Правила работы" } as const;

/** Правовая оговорка — одна на все страницы. */
export const LEGAL_NOTE =
  "Сайт носит рекламно-информационный характер и не является интернет-магазином. Указанные цены не являются публичной офертой.";

/**
 * Строка авторства в подвале — не подпись подрядчика, а приглашение.
 *
 * Подвал лендинга читают люди, которые пришли за ростом собственной
 * практики: у большинства из них сайта нет вовсе. Поэтому здесь не
 * «дизайн и разработка — студия», от чего нечего нажать, а вопрос в их
 * сторону и глагол. Второго бренда в правовой полосе не появляется:
 * строка читается репликой человека, а реквизиты выше остаются одни.
 *
 * Контактов два, и оба — мессенджеры: эта аудитория пишет в личку, а не
 * заполняет форму на корпоративном сайте. Telegram первым — там быстрее
 * отвечают; Instagram для тех, кто из него и не выходит.
 *
 * Telegram ведёт прямо в переписку — `t.me/<ник>` с готовым текстом
 * первого сообщения: человеку остаётся нажать «Отправить».
 *
 * Instagram — на профиль, а не на `ig.me/m/<ник>`: тот редирект открывает
 * Direct только у тех, кто уже вошёл, а гостя уводит на страницу входа —
 * с сайта это выглядит как сломанная ссылка. С профиля же кнопка
 * «Написать» на месте, и в строке браузера видно знакомый адрес.
 *
 * Метки `utm` здесь бесполезны: ни `t.me`, ни Instagram их не принимают
 * и не показывают. Откуда пришёл человек, видно иначе — по тексту
 * первого сообщения в Telegram; если понадобится считать клики, это
 * событие в аналитике, а не параметр в адресе.
 */
export const SITE_CREDIT = {
  note: "Нужен такой же сайт для вашей практики — напишите в",
  contacts: [
    {
      label: "Telegram",
      href: "https://t.me/VitaliKarpuk?text=Здравствуйте! Пишу с сайта Дарьи — нужен сайт для моей практики.",
    },
    {
      label: "Instagram",
      href: "https://instagram.com/vitalikarpuk",
    },
  ],
} as const;
